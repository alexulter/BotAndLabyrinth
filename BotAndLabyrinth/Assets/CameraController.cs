using UnityEngine;
using System.Collections;

public class CameraController : MonoBehaviour {

	// Use this for initialization
	
	//GameObject BotGO;
	void Start () {
	//BotGO = GameObject.Find("Bot");
	
	}
	
	// Update is called once per frame
	void Update () {
		//transform.position = new Vector3 (BotGO.transform.position.x, transform.position.y, BotGO.transform.position.z);
	}
}
